import React from "react";
import ProfileComponent from "./profile-component";

const ProfileContainer = ({ location }) => {
  return <ProfileComponent location={location} />;
};

export default ProfileContainer;
