export const POST_SIGNUP_USER = (req,res,next) => {
    try {
        res.json({message:"This is the signup route"})
    } catch (error) {

    }
}

export const POST_SIGNIN_USER = (req, res, next) => {
    try {
        res.json({message:"This is the signin route"})
    } catch (error) {
        
    }
}